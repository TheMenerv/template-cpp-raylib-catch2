# C++, RAYLIB AND CATCH2 TEMPLATE

## Setup

Rename <em>My_Project</em> in files `/CMakeList.txt` and `/src/CMakeList.txt`

`Reload CMake Project` after rename

## Run project

Project called `My_Project`

## Run tests

Tests called `Tests`